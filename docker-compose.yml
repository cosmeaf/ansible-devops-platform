# Ansible DevOps Platform — Module 1 (Core Platform)
#
# PostgreSQL and Redis are deliberately NOT published to the host: they are
# reachable only from inside the `ansible-network` bridge. Only the API is
# exposed, on a high port (47420 by default).

x-django-env: &django-env
  env_file: .env
  networks: [ansible-network]
  depends_on:
    ansible-postgres:
      condition: service_healthy
    ansible-redis:
      condition: service_healthy

services:
  ansible-postgres:
    image: postgres:16
    container_name: ansible-postgres
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ansible-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks: [ansible-network]

  ansible-redis:
    image: redis:7
    container_name: ansible-redis
    restart: unless-stopped
    command: ["redis-server", "--save", "60", "1", "--appendonly", "no"]
    volumes:
      - ansible-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks: [ansible-network]

  ansible-api:
    <<: *django-env
    build:
      context: .
      dockerfile: docker/django/Dockerfile
    image: ansible-devops-platform/ansible-api:${PLATFORM_VERSION:-dev}
    container_name: ansible-api
    restart: unless-stopped
    volumes:
      - ansible-workspace:/app/workspace
    ports:
      - "${ANSIBLE_API_PORT:-47420}:8000"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8000/api/v1/health/"]
      interval: 15s
      timeout: 5s
      start_period: 40s
      retries: 5

  ansible-celery:
    <<: *django-env
    build:
      context: .
      dockerfile: docker/django/Dockerfile
    image: ansible-devops-platform/ansible-api:${PLATFORM_VERSION:-dev}
    container_name: ansible-celery
    restart: unless-stopped
    # The worker runs the playbooks, so it needs the same workspace the editor
    # writes to — a copy per container would execute yesterday's file.
    volumes:
      - ansible-workspace:/app/workspace
    command: ["celery", "-A", "core", "worker", "-l", "INFO", "--concurrency", "2"]
    # CMD-SHELL, not CMD: the exec form gives no shell, so $HOSTNAME would be
    # passed through literally and the node would never be found.
    healthcheck:
      test: ["CMD-SHELL", "celery -A core inspect ping -d celery@$$HOSTNAME"]
      interval: 30s
      timeout: 10s
      start_period: 40s
      retries: 3

  ansible-celery-beat:
    <<: *django-env
    build:
      context: .
      dockerfile: docker/django/Dockerfile
    image: ansible-devops-platform/ansible-api:${PLATFORM_VERSION:-dev}
    container_name: ansible-celery-beat
    restart: unless-stopped
    # The schedule file goes to a writable path; /app is not writable by the
    # unprivileged container user.
    command:
      - celery
      - -A
      - core
      - beat
      - -l
      - INFO
      - --schedule
      - /var/run/ansible/celerybeat-schedule
    # Beat serves no HTTP, so the image's curl-based HEALTHCHECK can never pass
    # here and must be overridden. There is no `inspect ping` for beat either —
    # it is a scheduler, not a worker — so liveness is PID 1 still being beat.
    # Checked via /proc to avoid installing procps just for pgrep.
    healthcheck:
      test: ["CMD-SHELL", "grep -q beat /proc/1/cmdline"]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3

networks:
  ansible-network:
    name: ansible-network

volumes:
  ansible-workspace:
    name: ansible-workspace
  ansible-postgres-data:
    name: ansible-postgres-data
  ansible-redis-data:
    name: ansible-redis-data
