[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = test_*.py *_test.py tests.py
testpaths = tests
addopts =
    --strict-markers
    --strict-config
    -ra
markers =
    integration: requires live PostgreSQL and Redis
    slow: runs Ansible for real, so it takes seconds rather than milliseconds
